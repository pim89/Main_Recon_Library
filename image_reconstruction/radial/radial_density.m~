function dcf = radial_density(traj)
%% Calculate simple Ram-Lak radial density function

kdim=size(traj);
if numel(kdim)<5
    kdim(end+1:5)=1;
end
dcf=zeros(kdim(2:end));

ram_lak=linspace(-1,1,kdim(1)+1);ram_lak(end)=[];
ram_lak(end/2+1)=1/kdim(1);
% END
end