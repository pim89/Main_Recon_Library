function idim = idim_from_trajectory(traj)
%%Estimate the image dimensions from the k-space trajectory
% Image dimensions are encoded in the k-space trajectory bounds.

% Image space dimensions
gg.idim(1:2)=ceil(max(abs(k(1,:))));
gg.idim(3)=ceil(max(abs(k(3,:))));
if gg.idim(3)==0; gg.idim(3)=1;end
gg.idim=[gg.idim kdim(4:end)];

% END
end